apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-gitlab
  namespace: "{{ gitlab_namespace }}"
spec:
  isCA: false
  commonName: gitlab
  secretName: sct-gitlab-tls
  dnsNames:
    - "apps.cluster-7cjss.sandbox2040.opentlc.com"
    - "*.apps.cluster-7cjss.sandbox2040.opentlc.com"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging


